[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python manage.py migrate && python manage.py seed_rbac && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/v1/health/"
restartPolicyType = "ON_FAILURE"
