# Frontend service — use when Root Directory is empty (repo root).
# Backend service MUST use Root Directory = backend (uses backend/railway.toml).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.frontend"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
healthcheckPath = "/"
healthcheckTimeout = 300
