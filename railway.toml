# Monorepo: use TWO services. Do not deploy backend from repo root.
# Backend  → Root Directory: backend  (uses backend/railway.toml + Dockerfile)
# Frontend → Root Directory: frontend (uses frontend/railway.toml + Dockerfile)
