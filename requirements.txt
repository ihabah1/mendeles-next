# Used ONLY when Railway Root Directory is the REPO ROOT (not recommended).
# Prefer: Root Directory = backend + backend/Dockerfile
#
# If you deploy from repo root, this file pulls backend deps:
-r backend/requirements.txt
